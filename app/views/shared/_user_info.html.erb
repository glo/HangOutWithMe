<a href="<%= user_path(current_user) %>">
  <%= image_tag current_user.image, size: 52 %>
</a>
<h3>
  <%= current_user.name %>
</h3>
<span>
  <%= link_to "View my profile", current_user %>
</span>
<span> 
  (<%= pluralize(current_user.events.count, "event") %>)
</span>